{
  "name": "auto-update-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "live-server --open=./index.html || true",
    "build": "echo 'No build step configured'"
  }
}
